#include<stdio.h>
int main()
{
    // int m, n, p;

    // scanf("m=%dn=%dp=%d", &m, &n, &p);
    // printf("%d%d%d\n", m, n, p);
    // return 0;

    char s[] = "123", *p;
    int a = 4;
    p = s;
    printf("%d,%d", sizeof(p), sizeof(a));
    //return 0;
}

//有问